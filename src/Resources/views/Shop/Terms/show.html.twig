{% extends '@SyliusShop/layout.html.twig' %}

{%- block title -%}
    {{ terms.name }}
{%- endblock -%}

{% set elements = monsieurbiz_richeditor_get_elements(terms.content) %}

{% block content %}
    {% include '@SetonoSyliusTermsPlugin/Shop/Terms/Show/_breadcrumb.html.twig' %}

    <div class="ui hidden divider"></div>

    <div class="ui two column stackable grid">
        <div class="column">
            <h1>{{ terms.name }}</h1>

            {{ elements|monsieurbiz_richeditor_render_elements }}
        </div>
    </div>
{% endblock %}
